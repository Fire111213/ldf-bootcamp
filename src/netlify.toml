[build]
  command = "npm run build"
  publish = "build"  # For Create React App, the build output is in 'build' folder

# Handle client-side routing - redirect all routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200